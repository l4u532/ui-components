page area: Code block
path: /components/agents/code-block   root: #preview
browser: Safari 26.6.2 on macOS 26.6.2, driven by safaridriver (W3C WebDriver, port 4491)
viewport: 1440x900
method: scroll the root so its top sits 96 px below the viewport top, click its top-left padding at
  (root.left + 6, 102) to set WebKit's focus-navigation starting point, then press Tab 8 times and
  read document.activeElement after each press. A step counts as a scroll region when the focused
  element carries an overflow-auto / overflow-scroll class and is not a natively focusable control.

variant: before (upstream main, http://localhost:3300)
  url: http://localhost:3300/components/agents/code-block
  scroll regions inside the root before tabbing (element / tabIndex):
    tabIndex=-1  div "1import { generateText } from "ai";23exp"
  Tab sequence:
    Tab 1: button[aria-label="Copy code"]
    Tab 2: button[aria-label="Copy code"]
    Tab 3: a "Explore beUI Pro"
    Tab 4: a[aria-label="Star on GitHub"]
    Tab 5: a "Get Pro"
    Tab 6: button[aria-label="Copy code"]
    Tab 7: button[aria-label="Copy code"]
    Tab 8: a "Explore beUI Pro"
  scroll region reached: no, not within the first 8 tab stops
  screenshot: code-block-safari-before.png

variant: after (PR branch fix/scroll-region-tab-stops, http://localhost:3304)
  url: http://localhost:3304/components/agents/code-block
  scroll regions inside the root before tabbing (element / tabIndex):
    tabIndex=0  div "1import { generateText } from "ai";23exp"
  Tab sequence:
    Tab 1: button[aria-label="Copy code"]
    Tab 2: div "1import { generateText } from "ai";23exp"   <= SCROLL REGION (tabIndex 0, overflow auto/auto, actually scrolls: yes)
    Tab 3: button[aria-label="Copy code"]
    Tab 4: div "$ bunx --bun shadcn add @beui/code-block"   <= SCROLL REGION (tabIndex 0, overflow auto/auto, actually scrolls: no)
    Tab 5: a "Explore beUI Pro"
    Tab 6: a[aria-label="Star on GitHub"]
    Tab 7: a "Get Pro"
    Tab 8: button[aria-label="Copy code"]
  scroll region reached: yes, at Tab 2
  screenshot: code-block-safari-after.png

screenshots: code-block-safari-before.png and code-block-safari-after.png are the same viewport and the same
  number of Tab presses (2); after the fix the focused scroll region draws an inset ring.
