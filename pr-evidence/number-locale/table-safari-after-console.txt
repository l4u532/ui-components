page: /components/motion/table
url: http://localhost:3303/components/motion/table
variant: after (PR branch)
browser: Safari 26.6.2 on macOS 26.6.2, driven by safaridriver (WebDriver)
locale: macOS system locale de_DE   navigator.language: de-DE   Intl default: de-DE
viewport: 1440x900   document.visibilityState: visible
capture: Safari does not expose the console over WebDriver, so an in-page wrapper around
  console.error / console.warn / window onerror / unhandledrejection is installed as early as
  the protocol allows: the session uses pageLoadStrategy "none", so navigation returns at once
  and the wrapper is injected mid-load, at +75ms wall clock (installed@54).
  Installed after the normal load event (~+280ms) the wrapper misses this error entirely.
screenshot anchor: data-preview (scrollY 145)
grouped numbers rendered in that preview (after settle): 10,000

hydration-related messages: 0
total captured errors/warnings: 0

(no errors, warnings or uncaught exceptions captured)
